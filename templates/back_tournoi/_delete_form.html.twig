<form method="post" action="{{ path('app_back_tournoi_delete', {'id': tournoi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.id) }}">
    <button class="btn">Delete</button>
</form>
